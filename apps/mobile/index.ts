// Expo entry point. UNBUILT/UNTESTED — see package.json description and
// LIMITATIONS.md. This file is not verified by any typecheck or bundler run
// in this environment (no react-native types / Metro bundler available
// here); it follows the standard Expo `registerRootComponent` entry shape.
import { registerRootComponent } from "expo";
import App from "./App";

registerRootComponent(App);
