{
  "name": "@navia/core",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-independent NAVIA navigation engine: geodesy, GNSS integrity, sensor fusion, dead reckoning, map matching, route/landmark/confidence engines, AI tool-calling context, offline-aware provider interfaces. Pure TypeScript, no React Native / native module dependencies — importable by apps/mobile and testable directly in Node.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "tsx --test test/**/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.0"
  }
}
